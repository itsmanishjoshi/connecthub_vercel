import { Attendee } from '@/types/attendee';

interface StatsPanelProps {
  attendees: Attendee[];
}

export const StatsPanel = ({
  attendees,
}: StatsPanelProps) => {
  return null; // StatsPanel is no longer needed
};
